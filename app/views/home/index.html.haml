.tagline
  %h3
    The ups and downs of achieving your goals, shared openly, honestly, secretly.

.main
  = render "updates/stream"

.sidebar
  = render "shared/update_counts", :subject => nil, :subject_name => "their goals"

  .goals.cloud
    %h3 What's Your Goal?
    - @goals.shuffle.each do |goal|
      = link_to goal['title'], goal_path(goal['permalink']), :class => "size_#{goal['bucket']}"


